<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.good.mapper.GoodExhibitsMapper">

    <select id="queryPageList" resultType="org.jeecg.modules.good.dto.GoodExhibitsDTO">
              SELECT
            ge.*,(
            SELECT
                CONCAT( store_name, "(", sub_store_name, ")" )
            FROM
                `store_manage`
            WHERE
                id = ge.store_manage_id
                AND del_flag = "0"
                LIMIT 1
            ) AS storeName
        FROM
            good_exhibits ge
        ${ew.customSqlSegment}
    </select>
</mapper>