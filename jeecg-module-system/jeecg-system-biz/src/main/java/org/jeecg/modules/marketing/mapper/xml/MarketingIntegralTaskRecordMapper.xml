<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.marketing.mapper.MarketingIntegralTaskRecordMapper">
    <select id="queryPageList" resultType="org.jeecg.modules.marketing.vo.MarketingIntegralTaskRecordVO">
        SELECT
          *
        FROM
          (SELECT
            mitr.*,
            ml.`nick_name`,
            ml.`phone`
          FROM
            marketing_integral_task_record mitr
            LEFT JOIN member_list ml
              ON mitr.`member_list_id` = ml.`id`
          WHERE mitr.`del_flag` = 0
          ORDER BY mitr.create_time DESC
          ) m ${ew.customSqlSegment}
    </select>
</mapper>