<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.store.mapper.StoreSeriesManageMapper">


    <select id="getStoreSeriesManageByParentId" resultType="map">
					SELECT
				id,
				series_name AS seriesName,
				series_abstract AS seriesAbstract,
				picture
			FROM
				store_series_manage
			WHERE
				del_flag = '0'
				AND parent_id = #{parentId}
			ORDER BY
				sort
	</select>
</mapper>