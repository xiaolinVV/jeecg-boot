<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.marketing.mapper.MarketingFreeAdvertisingMapper">
    <select id="selectMarketingFreeAdvertisingIndex" resultType="map">
            SELECT
            advertising_title AS advertisingTitle,
            image,
            jump_type AS jumpType,
            address
            FROM
            `marketing_free_advertising`
            WHERE del_flag = '0'
            AND STATUS = '1'

            AND START_TIME &lt;=NOW()
            AND END_TIME &gt;=NOW()

            ORDER BY sort ASC

    </select>
</mapper>